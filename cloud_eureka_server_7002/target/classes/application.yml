server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002.com #eureka服务端的实例名称
  client:
    # false表示不向注册中心注册自己
    register-with-eureka: false
    fetch-registry: false
    service-url:
      # 与Eureka Server交互的地址，后期服务查询和注册都需要用到这个地址
      defaultZone: http://eureka7001.com:7001/eureka/

